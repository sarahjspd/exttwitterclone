$("#likeajax-<%= @twee.id %>").html("<%= j render 'tweets/likeajax', twee: @twee %>")

// This file needs to be in the same view folder as the function. In this case, it is create in Likes. The variables taken from here is taken from the controller.